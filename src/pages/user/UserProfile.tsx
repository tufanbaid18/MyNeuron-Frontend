import React from "react";
import UserProfileManager from "../../components/user/ProfileBuilder/UserProfileManager";

const UserProfile: React.FC = () => {
  return <UserProfileManager mode="profile" />;
};

export default UserProfile;
